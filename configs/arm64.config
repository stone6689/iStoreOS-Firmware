# iStoreOS arm64 Target Configuration
# Based on official iStoreOS build for armvirt/64 (aarch64_generic)
# Do NOT add extra packages — keep identical to official build

CONFIG_TARGET_armvirt=y
CONFIG_TARGET_armvirt_64=y
CONFIG_TARGET_armvirt_64_DEVICE_generic=y
