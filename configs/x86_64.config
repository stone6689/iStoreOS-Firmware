# iStoreOS x86_64 Target Configuration
# Based on official iStoreOS build for x86/64
# Do NOT add extra packages — keep identical to official build

CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_64=y
CONFIG_TARGET_x86_64_DEVICE_generic=y
